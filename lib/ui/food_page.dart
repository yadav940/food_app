//create by vinod.yadav940@gmail.com at 14/5/21
import 'package:flutter/material.dart';

class FoodPage extends StatelessWidget {
  bool value = false;
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: Text('Details'),
        ),
        body: ListView(
          children: [

          ],
        ),
      ),
    );
  }
}
